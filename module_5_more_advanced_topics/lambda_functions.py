#!/usr/bin/env python3

# 01: Write a filter with a lambda function that will filter out all values from the list that are 
# greater than 5.
ex_1_list = [1, 2, 3, 4, 5, 6, 7, 8, 9]

# 02: Write a map with a lambda function that will square every value in a list.
ex_2_list = [1, 2, 3, 4, 5, 6, 7, 8, 9]

# 03: Write a map with a lambda function that will take every item in a list and append the string 
# ".com" to it.
ex_3_list = [55, 'google', 'facebook', 19.7, True]

# 04: Write a filter with a lambda function that will filter out all values from a list whose 
# length is greater than 1.
ex_4_list = ["a", "", "abc", "abcdef", "y", ""]

# 05: Write a filter with a lambda function that will remove all items from the list below that do 
# not contain the "name" key.
ex_5_list = [
    {
        "name": "Isaiah",
        "enrolled": True
    },
    {
        "name": "Shaedon",
        "assigned": False
    },
    {
        "is_cat": True,
        "pet_name": "Rudy"
    },
    {
        "id": "Jordan",
        "number": 19
    }
]
